# Chain configuration
# Set to "local" to use local development settings
# Any other value will use production/testnet settings
CHAIN=local

# RPC URL (used when CHAIN is not "local")
# If not set, defaults to https://rpc.ankr.com/monad_testnet
RPC_URL=https://rpc.ankr.com/monad_testnet

# Port for the API server (optional, defaults to 3001)
PORT=3001